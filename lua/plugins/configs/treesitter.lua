local options = {
  ensure_installed = {
    "lua",
    "vim",
    "bash",
    "markdown",
    "markdown_inline",
    "regex"
  },
  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
